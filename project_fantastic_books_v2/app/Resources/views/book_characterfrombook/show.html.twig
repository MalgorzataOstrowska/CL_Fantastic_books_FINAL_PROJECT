{% extends 'base.html.twig' %}

{% block body %}
    <h1>Book_characterfrombook</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book_CharacterFromBook.id }}</td>
            </tr>
            <tr>
                <th>Charactertype</th>
                <td>{{ book_CharacterFromBook.characterType }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('editor_book_characterfrombook_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('editor_book_characterfrombook_edit', { 'id': book_CharacterFromBook.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
