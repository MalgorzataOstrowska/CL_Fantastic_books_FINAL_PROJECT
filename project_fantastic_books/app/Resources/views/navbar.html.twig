<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="">Fantastic Books</a>
        </div>
        <ul class="nav navbar-nav">
            <li><a href="{{ path('fantasticbooks_default_index') }}">Home</a></li>

            {% if is_granted('ROLE_EDITOR') %}
                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown">
                        {{ 'Character'| trans }}
                        <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li>
                            <a href="{{ path('editor_bookcharacter_index') }}">
                                {{ 'index'| trans }}
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('editor_bookcharacter_new') }}">
                                {{ 'new'| trans }}
                            </a>
                        </li>
                    </ul>
                </li>
                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown">
                        {{ 'Author'| trans }}
                        <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li>
                            <a href="{{ path('editor_author_index') }}">
                                {{ 'index'| trans }}
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('editor_author_new') }}">
                                {{ 'new'| trans }}
                            </a>
                        </li>
                    </ul>
                </li>
                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown">
                        {{ 'Series'| trans }}
                        <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li>
                            <a href="{{ path('editor_series_index') }}">
                                {{ 'index'| trans }}
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('editor_series_new') }}">
                                {{ 'new'| trans }}
                            </a>
                        </li>
                    </ul>
                </li>
                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown">
                        {{ 'Books'| trans }}
                        <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li>
                            <a href="{{ path('editor_book_index') }}">
                                {{ 'index'| trans }}
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('editor_book_new') }}">
                                {{ 'new'| trans }}
                            </a>
                        </li>
                    </ul>
                </li>
            {% endif %}
        </ul>
        <ul class="nav navbar-nav navbar-right">


            {% if app.user %}
                <li>
                    <a href="{{ path('fos_user_profile_show') }}">
                        <span class="glyphicon glyphicon-user"></span>
                        {{ 'layout.logged_in_as'|trans(
                            {'%username%': app.user.username | default('TEST')}, 'FOSUserBundle'
                        ) }}
                    </a>
                </li>
            {% endif %}

            <li>
                <a href="{{ path('fos_user_registration_register') }}">
                    <span class="glyphicon glyphicon-user"></span>
                    {{ 'layout.register'|trans({}, 'FOSUserBundle') }}
                </a>
            </li>

            {% if app.user %}
                <li>
                    <a href="{{ path('fos_user_security_logout') }}">
                        <span class="glyphicon glyphicon-log-out"></span>
                        {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                    </a>
                </li>
            {% else %}
                <li>
                    <a href="{{ path('fos_user_security_login') }}">
                        <span class="glyphicon glyphicon-log-in"></span>
                        {{ 'layout.login'|trans({}, 'FOSUserBundle') }}
                    </a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>